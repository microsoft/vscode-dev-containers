{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "environment-install",
			"type": "shell",
			"command": "umask 0002 && /opt/conda/bin/conda env update -n base -f ${workspaceFolder}/test-project/environment.yml"
        }
    ]
}