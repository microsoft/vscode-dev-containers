{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pip install",
            "type": "shell",
            "command": "pip install -r ./test-project/requirements.txt"
        }
    ]
}
